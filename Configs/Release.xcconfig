MARKETING_VERSION = 1.5.1
CURRENT_PROJECT_VERSION = 4

IPHONEOS_DEPLOYMENT_TARGET = 16.0
MACOSX_DEPLOYMENT_TARGET = 13.0
SWIFT_VERSION = 5.0

// Disable Swift symbol generation for String Catalogs (keys like "%@" and "—" are invalid Swift identifiers)
SWIFT_EMIT_LOC_STRINGS = NO

DEVELOPMENT_TEAM = 7SF8L3TAFC
CODE_SIGN_STYLE = Automatic

PRODUCT_BUNDLE_IDENTIFIER = chat.bitchat.dstealth

// Optional include of secrets (API keys) for release builds
#include? "Secrets.xcconfig"
